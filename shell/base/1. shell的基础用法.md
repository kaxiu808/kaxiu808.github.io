

# shell介绍

- 什么是shell
- shell功能
----
shell脚本介绍

###  
日常运维工作大部分是一些繁的重复的工作，每天80%的工作就是重复干昨关的事情，而且工作量还比较大，那么摆在运维面前的选择就是

A：自己搞定一切 
B：让机器搞定一切

当然，谁不愿意喝看茶水，逗看前台妹子过完愉快的一关呢！如何让机器搞定一切，得以解放自己已，就是我们本门课程的主旨。shell脚本是你入门最快、最高效的一个脚本语言。
我主要给大家讲解了shell的语法及应用，还有大量的案例让大家去学习，去思考！希望天家能学好shell的同时去写好shell，

shell学习分成了几个阶段，大家遂步提升就好。
1）能看到shell脚本
2）能改shell脚本
3）能自己写shell脚本 
4）能优化shell脚本

##  一、什么是shell

shell是一个程序，采用C语言编写，是用户和linux内核沟通的桥梁。它既是一种命令语言，又是一种解释性的编程语言。通过一个图表来查看一下shell的作用。
![shell图片](https://gitlab.com/minika1/Pic/-/raw/main/pictures/2024/03/9_18_18_3_shell%E5%9B%BE%E7%89%87.png)

```shell
kernel:为软件服务，接收用户或软件指令驱动硬件，完成工作；  是内核，即机器语言 是0和1
shell：命令解释器
user：用户接口，对接用户。
```
上图可以看出，shell在操作系统中起到了承接用户和系统内核的作用。那为什么不直接用户对内核呢
原因很简单，因为内核处理的都是二进制，而用户处理的都是高级语言。简而言之，如果没有shell，你希望告诉你喜欢的妹子：我爱你。你需要经过以下步骤：
1）将“我爱你”翻译成二进制
2）告诉内核
3）内核通过网卡发送给你的妹子
4）妹子计算机网卡收到你发的二进制
5）网卡交给内核 
6）内核交给妹子
7）妹子看到都是一串01组成的数字，o my god，二进制不是人人都懂的，你的表白也就石沉大海了。
为了让所有人都能快速、方便的使用计算机，我们的系统开发人员通过shel解决了这个问题。使任何一个希望通过计算机来工作、娱乐的人都能够快速操作计算机。

##  二、shell功能
	 - 命令行解释功能
	 - 启动程序
	 - 输入输出重定向
	 - 管道连接
	 - 文件名置换(echo /*)
	 - 变量维护
	 - 环境控制
	 - shell编程


## shell语法
	- 如何抒写一个shell脚本
	- shell脚本运行
	- shell中的特殊符号
	- 管道
	- 重定向
	- shell中数学运算
	- 脚本退出
----
shell 脚本就是将完成一个任务的所有的命令按照执行的先后顺序，从上而下写入到一个文本文件中，然后给予执行权限。  

###  一、  如何抒写shell脚本
> 新建文件夹 mkdir shell_03
> cd shell_03
怎么理解上述这两句 ，首先要一个文本文件 
 vim nginx_install.sh      
 yum -y install vim

yum -y install wget gcc pcre-devel zlib-devel
weget http://nginx.org/download/nginx-1.16.0.tar.gz
tar xf nginx-1.16.0.tar.gz
cd nginx-1.16.0
./configure  --prefix=usr/local
make  -j 4      cpu调动执行4个线程
make install

然后给予文件权限
vim nginx_install.sh
chmod 700 nginx_install.sh      chmod: 权限
./nginx_install.sh

查看文件按照情况
cd  /opt/shell/shell_03/
cat nginx
cat nginx_install.sh
![从上到下执行文件](https://gitlab.com/minika1/Pic/-/raw/main/pictures/2024/03/9_19_10_0_%E6%96%87%E4%BB%B6%E6%89%A7%E8%A1%8C%E6%AD%A5%E9%AA%A4%E9%A1%BA%E5%BA%8F.png)


###  shell脚本运行方法

###  shell中的符号

###  管道

###  重定向

###  shell脚本中的数学运算

###  脚本退出


##  格式化输出

###  echo语法

###  颜色字体

##  格式化输入

###  read命令


##  shell变量

### 变量介绍
### 变量分类
### 变量管理

## shell数组

### 数组介绍
### 默认数组
### 关联数组

## shell五大运算

### 赋值运算
### 数学运算
### 比较运算
### 关系运算
###  文件类型比较

## 流程控制

### 流程控制-if判断
	- 单if语法
	- if...else语法
	- if...elif...else语法
	- if高级用法

### 流程控制-for循环
	- 循环介绍
	- for语法
#### 循环控制
    - sleep
    - continue
    - break
      
### 流程控制-while循环
	 - while循环介绍
	 - while语法
	 - while实战 
		

### 流程控制-until循环
	 - until介绍
	 - until语法
### 流程控制-case
	- case介绍
	- case语法
	- 特殊变量
###  shell函数
	- 函数介绍
	- 函数用法
	- 函数应用
###  正则表达式
	- 正则表达式介绍
	- 特殊字符
	- POSIX字符
###  shell对文件的操作
	- shell对文件操作介绍
	- sed命令
	- sed小技巧
###  shell对输出流的处理
    - awk介绍
    - awk语法
    - awk高级用法
    - awk技巧
###  shell案例

			
<!--stackedit_data:
eyJoaXN0b3J5IjpbLTI3ODIyMjg1NCw1NjcyMDUwNTksMTgxMT
kyNzgwMyw3MzIwNzg3NjgsLTcwODc2MzU4MywxMDA2NzU2NDUs
LTEwNjAzMTk2NTddfQ==
-->